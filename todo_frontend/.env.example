# Example environment variables for the To-Do frontend
# Copy to .env.local and adjust values for local development.

# Base URL for the backend API (should include /api if your backend is namespaced)
REACT_APP_API_BASE_URL=http://localhost:4000/api
